# Nine-goal completion matrix

Target client: `1.182.03.301371`; application data: `1.145.01`.

| Goal | Delivered runtime | Verifiable evidence | Boundary |
|---|---|---|---|
| Official-grade battle | Deterministic server-authoritative timeline, 79 campaign chapter lookups, tower/boss/raid stages, level/quality/rank stats, animation timing, energy, skills, ultimate, shield, heal, control, armor/MR, crit and haste; client result forgery is rejected | `game_battle.js` (9,461,126 bytes) plus Unit/Stage/Anim tables and their SHA-256 manifest | The official private battle server was never shipped. The local executor is independently implemented and keeps the recovered client engine as its oracle; it is not represented as leaked server code. |
| Exact dynamic values | All 866 decrypted gameplay configuration tables are lazy-loaded with hashes; old 30-table paths were replaced in core systems | `runtime/client-evidence-manifest.json` | Match pools, fraud rules, experiments and live schedules are server-private and therefore configurable local policy. |
| Base systems | Dedicated loops for growth, stage, idle, task, shop, mail, bag, tavern, tower, maze, arena, guild, chat/friend; local-authoritative domain execution for equipment, artifact, bounty, formation, pet, home, furniture, album, mercenary and every remaining protocol module | MySQL tables plus `/__afk/game/action` and `/__afk/systems/action`; exhaustive execution test for all 1,599 recovered operations | Domain modules persist typed progress, costs, rewards, claims, sessions and records without inventing undocumented official matchmaking data. |
| Advanced modes | Persistent domain state machines for legend/top arena, GVG, raid, trial, world boss, SLG, infinite PVE/boss, curse land, idle PVP, side stories, camp PVP, COF and other recovered modules; local network ecology has 120 persistent bots and six bot guilds | Protocol catalog has 184/184 modules, 1,599/1,599 executable operations and zero generic/unimplemented routes; bot battles use official Unit growth tables and the authoritative executor | Mode-specific opponents and schedules are generated by configurable local authority. |
| Timed events | Local activity scheduler, daily/weekly/season resets and live-ops instances generated for every activity-class protocol, using matching official config tables where present | `liveops_instances`, `/__afk/liveops/instances`, admin upsert endpoint | Dates are local schedules because official production calendars are not client artifacts. |
| Recharge | Native `SDKInterface.pay` is redirected to an idempotent local instant-grant bridge; 161 recovered visible ChargeGoods rows expand directly into transactional currency, item or hero entitlements | `payment_orders`, `payment_entitlements`, `/__afk/payments/purchase`, `scripts/afk-instant-pay-bridge.py` | Local test mode only: no checkout, real-money collection, or fabricated App Store/Google/official receipt. |
| Visual GM | Responsive standalone admin UI for health, players, assets/stage/mail/ban/reset, protocol catalog, live ops, payments and audit | `/gm-ui/`; GM/admin bearer authentication and audit table | Separate from the player's client frontend. |
| Fully offline resources | Strict-offline server covers extracted patch/timely/extra/language/version roots and APK; newer official packages are synchronized into versioned quarantine and identity-checked before activation | Current live tree: 47,326 files, 3,782,740,433 bytes, aggregate SHA-256 `323cc8d3eadb070d3c1a818df60b1a84ac33871a1f323c6b4d8de38cdad95768`; latest acquisition report lives under `runtime/official-updates/` | A newer resource tree is never overlaid onto an older protocol client until compatibility validation passes. |
| Production operations | TLS listener, security headers, auth, rate limiting, metrics, structured logs, health supervisor, alert transitions/webhook, automatic restart, checksummed primary/offsite backups, guarded restore and isolated restore drill | `/__afk/health`, `/__afk/metrics`, `runtime/production-readiness.json`, and a verified 24-table isolated restore | This is a single-host private deployment. Multi-region quorum/managed payment compliance require external infrastructure outside this workspace. |

## Protocol closure

- Recovered top-level request modules: 184.
- Dedicated integrations: 13.
- Partially specialized modules: 6.
- Local-authoritative domain integrations: 165.
- Recovered executable operations: 1,599/1,599.
- Protobuf reply message definitions: 2,587.
- Stateful generic modules: 0.
- Unimplemented wire routes: 0.

Every route uses the recovered `up.proto`/`down.proto` envelope and a schema-valid typed reply. Domain routes persist local-authoritative progress, resources, claims, battle sessions and history. This prevents client requests from hanging while keeping the distinction between recovered protocol facts and unknowable official server decisions explicit.
